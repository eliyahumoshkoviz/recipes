/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            
            hostname: 'www.eatingwell.com',
           
          },
        ],
      }
};

export default nextConfig;
